# 资源检测并发改造设计说明

状态：已完成

本文档记录当前项目中“论文检测”和“同行评审 Review 检测”链路的并发改造背景、设计边界、实际完成项、测试覆盖和剩余限制。

本文档定位是：
- 管理说明文档
- 架构设计与变更记录
- 资源检测链路的维护参考

本文档不再作为待执行的实施计划使用。

## 1. 适用范围

本文档仅覆盖资源检测链路：
- 论文检测 `task_type=paper`
- 同行评审 Review 检测 `task_type=review`

不覆盖：
- 图像检测主链路
- 人工审核链路
- 管理员审核分配链路

## 2. 改造背景

资源检测在改造前已经具备：
- 异步提交任务
- 文本预处理与检测
- 结果持久化与报告生成

但存在以下工程性问题：
- 资源任务通过裸线程启动，并发数不可控
- 含 LLM 的资源任务和普通资源任务没有隔离
- `paper` / `review` 接口允许传多个 `file_ids`
- 但执行函数实际上只处理首个资源或首个 pair

这会导致：
- 多用户同时提交 paper/review 任务时线程数不可控
- 资源任务会与图像任务一起挤占 Django 进程
- API 表面支持批量，实际不是真正的批量资源检测

## 3. 改造边界

本次改造目标不是重写资源检测算法，而是补齐“批量 + 并发”的执行能力。

允许改动的部分：
- 资源任务调度方式
- 资源任务执行池划分
- LLM 资源任务隔离
- 多资源任务聚合结果结构
- 自动化测试
- 前端资源结果页的展示增强
- 说明文档

不主动改动的部分：
- 段落分析算法本身
- 参考文献分析算法本身
- Review 相关度/资格判断规则
- 现有前端整体风格与设计语言

## 4. 正确性约束

### 4.1 结果语义不变

必须保持：
- `paragraph_results`
- `reference_results`
- `overall_evaluation`
- `review_analysis_results`
- `relevance_results`

这些字段在单资源语义下的含义不变。

### 4.2 文本覆盖逻辑不变

必须保持：
- `text_override`
- `paper_text_override`
- `review_text_override`

在执行前后仍然按原语义工作。

### 4.3 图像子链路复用不变

论文检测中如果开启抽图检测，仍然复用已改造好的图像检测链路。

## 5. 当前真实生效链路

当前资源检测主链路：
- 提交入口：`AIDetector/code/backend/backend-code/core/views/views_dectection.py`
- 资源任务编排：`AIDetector/code/backend/backend-code/core/services/orchestrators/resource_task_orchestrator.py`
- 论文检测执行：`AIDetector/code/backend/backend-code/core/services/orchestrators/paper_task_orchestrator.py`
- Review 检测执行：`AIDetector/code/backend/backend-code/core/services/orchestrators/review_task_orchestrator.py`

当前执行方式：
- 资源任务异步提交
- 普通资源任务进入受控执行池
- 含 LLM 的资源任务进入独立串行池
- `paper` 任务支持一个任务处理多个 `paper` 文件
- `review` 任务支持一个任务处理多个合法 `review_paper + review_file` pair

## 6. 实际完成的改造项

### 6.1 资源任务执行池化

已完成：
- 用 `ThreadPoolExecutor` 替代裸线程
- 引入普通资源任务执行池
- 引入 LLM 资源任务串行执行池

效果：
- 多用户提交 paper/review 任务时，并发数可控
- LLM 资源任务不会与普通资源任务抢同一个执行池

### 6.2 资源任务真批量执行

已完成：
- `run_paper_detection_task()` 可逐个处理多个 `paper` 文件
- `run_review_detection_task()` 可逐个处理多个合法 review pair

效果：
- `paper` / `review` 任务不再只是“表面支持多个 file_ids”
- 一个任务内的多个资源会被真正处理

### 6.3 任务级聚合结果结构

已完成：
- 多资源结果统一聚合到 `text_detection_results["items"]`
- 原有单资源字段继续保留，默认镜像第一个资源结果

效果：
- 兼容现有前端和报告
- 同时保留全量批量结果数据

### 6.4 专用结果模型兼容

已完成：
- `PaperDetectionResult` / `ReviewDetectionResult` 继续保留
- 当前仍主要镜像第一个资源结果
- 在有专用结果模型时，`items` 也不会丢

效果：
- 不需要一次性重构所有资源结果模型
- 后端状态接口与结果接口都能兼容多资源结果

### 6.5 前端多资源可见化

已完成：
- 资源详情页显示资源数
- `PaperResultView` 显示多资源总览
- `ReviewResultView` 显示多资源总览
- 两个结果页都支持“按资源切换查看”

效果：
- 用户不再只能看到第一个资源的结果
- 资源检测前端已具备基础多资源浏览能力

说明：
- 本次增强保持了现有 Vuetify 风格
- 没有重做整体页面视觉体系

### 6.6 报告多资源概览

已完成：
- paper/review 报告生成中增加“批量资源总览”节

效果：
- 多资源任务的报告不再完全只体现第一份资源

## 7. 测试覆盖

当前已验证通过的资源检测关键测试方向包括：
- 资源任务执行池与 LLM 任务分流
- 多 paper 文件聚合
- 多 review pair 聚合
- 结果接口直接透出 `items`
- 在专用结果模型存在时仍保留 `items`

当前相关测试文件：
- `AIDetector/code/backend/backend-code/core/tests/test_resource_task_concurrency.py`
- `AIDetector/code/backend/backend-code/core/tests/test_resource_preprocessing.py`
- `AIDetector/code/backend/backend-code/core/tests/test_resource_task_flow.py`

## 8. 当前默认配置口径

建议默认值：
- `RESOURCE_TASK_MAX_WORKERS=2`

说明：
- 资源任务的主要压力来自文本分析和 LLM 调用
- 初始阶段建议保持保守并发

## 9. 剩余限制

虽然资源检测链路已经具备批量与并发基础，但仍有一些剩余限制：

- 资源结果的专用 `OneToOne` 模型仍以第一个资源为主
- 当前前端是“多资源可见化 + 兼容型详情查看”，还不是独立的多资源结果管理器
- 资源预处理中仍有少量历史测试与当前实现存在漂移
- LLM 资源任务仍依赖外部模型或服务配置

## 10. 相关文档

- [README.md](./README.md)
- [图像检测并发改造设计说明.md](./图像检测并发改造设计说明.md)
- [图像检测联调与上线检查清单.md](./图像检测联调与上线检查清单.md)

## 11. 结论

本次改造完成后，资源检测链路已经从“异步提交但非真正批量、且并发不可控”的状态，升级为：
- 受控并发执行
- LLM 任务隔离
- 真批量资源处理
- 后端结果聚合
- 前端多资源基础可见化

如果后续继续演进，优先建议关注：
- 资源结果模型的进一步原生多资源化
- 资源检测联调与上线清单
- 资源预处理历史测试的统一校准
