# 图像检测并发改造设计说明

状态：已完成

本文档记录当前学术 AI 鉴伪项目图像检测链路的并发改造背景、设计边界、实际完成项、测试覆盖和剩余限制。

本文档的定位是：
- 管理说明文档
- 架构设计与变更记录
- 后续维护和组内同步材料

本文档不再作为待执行的实施计划使用。  
具体联调与部署请参考：
- [README.md](./README.md)
- [图像检测联调与上线检查清单.md](./图像检测联调与上线检查清单.md)

## 1. 改造背景

图像检测主链路在改造前已经具备：
- 批量提交图片
- 后台异步提交
- 本地推理与远程 GPU 推理两种模式

但当时存在以下工程性问题：
- 后端任务通过裸线程启动，并发数不可控
- 本地桥接层使用共享 `img.zip` / `data.json`
- 远程 GPU 服务也使用共享输入目录
- `pipeline_single_image.py` 使用共享缓存目录并会直接 `rmtree`
- LLM 路径写固定输出目录，并带有共享运行环境副作用

这些问题会导致：
- 多任务并发时可能串数据
- 不同请求可能互相覆盖输入文件
- cache 目录可能互相删除
- Django 进程的后台线程数量不可控

## 2. 改造边界

本次改造只针对“图像检测执行架构”，不改算法语义。

允许改动的部分：
- 任务调度方式
- 本地/远程推理桥接层
- 临时目录与缓存目录组织方式
- 并发控制与资源上限
- 自动化测试
- 相关说明文档

不主动改动的部分：
- 各检测方法本身的算法逻辑
- 当前 `payload` 的结构和方法名
- `DetectionResult` / `SubDetectionResult` 的字段语义
- 总体真假判断逻辑
- 报告生成逻辑
- 前端接口入参与出参结构

## 3. 正确性约束

改造过程中保持不变的关键约束如下：

### 3.1 输入与批次组织不变

- 用户仍通过 `/api/detection/submit/` 提交 `image_ids`
- 每张图仍创建或更新 `DetectionResult`
- 每个批次中的图片仍按 `image_upload.id` 排序写入 zip
- zip 内文件名仍使用现有 `8` 位数字命名规则

### 3.2 AI service 返回结构不变

- `local_infer.py` 仍输出 `start results` + base64 pickle
- `pipeline_single_image.py` 仍按当前方法顺序输出结果
- `SingleImageMethod.get_method_map()` 的方法名保持不变
- 后端 `_extract_single_result()` 的解析假设不变

### 3.3 落库语义不变

- `DetectionResult` 的真假、置信度、LLM 结论、ELA、EXIF 字段语义不变
- `SubDetectionResult.method` 保持原映射：
  - `urn_coarse_v2 -> splicing`
  - `urn_blurring -> blurring`
  - `urn_brute_force -> bruteforce`
  - `urn_contrast -> contrast`
  - `urn_inpainting -> inpainting`
- `DetectionTask` 仍在全部图片完成后再标记为 `completed`

### 3.4 失败语义不变

- 推理异常仍会标记任务失败
- 用量回退逻辑仍保留
- 错误消息传播路径不变

## 4. 当前真实生效链路

当前图像检测主链路：
- 提交入口：`AIDetector/code/backend/backend-code/core/views/views_dectection.py`
- 任务编排：`AIDetector/code/backend/backend-code/core/services/orchestrators/image_task_orchestrator.py`
- 图像检测执行：`AIDetector/code/backend/backend-code/core/services/capabilities/image/local_detection.py`
- 本地/远程推理桥接：`AIDetector/code/backend/backend-code/core/services/capabilities/image/local_inference_client.py`
- 本地推理入口：`AIDetector/code/ai-service/ai-service-code/local_infer.py`
- 远程 GPU 服务入口：`AIDetector/code/ai-service/ai-service-code/gpu_infer_service.py`
- AI pipeline：`AIDetector/code/ai-service/ai-service-code/pipeline/pipeline_single_image.py`
- 方法装配层：`AIDetector/code/ai-service/ai-service-code/method/SingleImageMethod.py`

当前真实执行方式：
- 前端可以一次提交多张图片
- 后端会将多张图片自动拆分成多个单独的图像检测任务
- 普通图像任务进入受控并发执行池
- 含 LLM 的图像任务进入独立串行执行池
- 每个单独图像任务内部仍按当前图像检测链路执行
- 本地与远程模式都使用 request 级输入目录
- 本地与远程模式都使用 request 级 cache root
- 远程 GPU 服务支持并发上限参数化，默认建议为 `1`

## 5. 实际完成的改造项

### 5.1 后端任务执行池化

已完成：
- 用固定大小的 `ThreadPoolExecutor` 替代“每个任务直接起线程”
- 普通图像任务使用可控并发池
- LLM 图像任务使用独立串行池

效果：
- 后端不会因为大量任务直接无限起线程
- 普通任务和 LLM 任务不会共享同一个执行池

### 5.1.1 多图输入自动拆分任务

已完成：
- 当用户一次提交多个 `image_ids` 时，后端会自动拆成多个单独图像任务
- 接口会继续返回第一个 `task_id` 作为兼容值
- 同时返回 `task_ids` 与 `task_count`

效果：
- 图像检测与资源检测的任务语义保持一致
- 单任务详情页与报告视图仍保持单资源模式

### 5.2 本地桥接层 request 目录隔离

已完成：
- `get_result()` 每次调用创建独立 request 目录
- `img.zip` 与 `data.json` 不再写入共享根目录
- 调用结束后清理 request 目录

效果：
- 多个本地推理请求不会互相覆盖输入文件

### 5.3 本地 AI service cache 隔离

已完成：
- `local_infer.py` 支持读取 request 级工作目录
- `pipeline_single_image.py` 的 cache root 改为优先读取环境变量
- 上游会为每次请求传入独立 `AI_SERVICE_CACHE_ROOT`

效果：
- 不再使用共享全局 `ai_image_cache`
- 并发请求不会互相删除 cache 目录

### 5.4 远程 GPU 服务 request 目录隔离

已完成：
- `gpu_infer_service.py` 每次请求创建独立 request 目录
- 不再直接写共享 `SHARED_DIR/img.zip` / `data.json`
- 请求结束后清理 request 目录

效果：
- 远程模式也具备输入目录隔离

### 5.5 远程 GPU 服务并发上限参数化

已完成：
- 将原来的全局串行锁升级为基于上限的控制器
- 环境变量：`AI_REMOTE_INFER_MAX_CONCURRENCY`
- 默认建议值保持为 `1`

效果：
- 默认行为与原来串行逻辑等价
- 后续如需放量，可逐步调高并发上限

### 5.6 LLM 路径调度与输出隔离

已完成：
- 含 LLM 的图像任务从调度层单独串行化
- LLM 的输出目录改到 request 级 cache root 下

效果：
- LLM 路径不再和普通图像任务共享执行池
- LLM 中间输出不再写固定共享目录

## 6. 新增与保留的测试

当前保留并通过的测试方向：
- 单图检测完整链路
- 多图检测结果映射顺序
- 本地桥接返回解析
- 远程桥接返回解析
- 失败状态和用量回退

本次新增的保障测试包括：
- `get_result()` 每次调用使用不同 request 目录
- request 目录在调用后会清理
- 普通图像任务与 LLM 图像任务使用不同 executor

当前并发相关测试文件：
- `AIDetector/code/backend/backend-code/core/tests/test_image_detection_flow.py`
- `AIDetector/code/backend/backend-code/core/tests/test_image_detection_concurrency.py`

## 7. 当前默认配置口径

当前建议默认值：
- `IMAGE_TASK_MAX_WORKERS=2`
- `AI_REMOTE_INFER_MAX_CONCURRENCY=1`

说明：
- 先保证稳定
- 先保证目录隔离和调度隔离
- 再视 GPU 资源情况决定是否放量

## 8. 剩余限制

虽然本次改造已经解决了主要并发风险，但仍有一些剩余限制：

- `SingleImageMethod.py` 仍然较重，初始化成本较高
- LLM 路径仍依赖额外模型与环境
- LLM 路径仍有运行时依赖安装行为
- 自动化测试未覆盖真实 GPU 权重推理
- 批次大小当前仍固定为 `20`

因此当前最适合的使用口径是：
- 普通图像任务可以受控并发执行
- LLM 任务继续保守使用

## 9. 相关文档

与本说明文档配套的材料：
- [README.md](./README.md)
- [图像鉴伪模块说明.md](./图像鉴伪模块说明.md)
- [图像检测联调与上线检查清单.md](./图像检测联调与上线检查清单.md)
- [AIDetector/code/ai-service/AI推理服务启动方式.md](./AIDetector/code/ai-service/AI推理服务启动方式.md)

## 10. 结论

本次并发改造完成后，当前项目的图像检测链路已经从“具备批量能力但存在明显共享资源风险”的状态，升级为“具备受控并发基础设施、目录隔离、调度隔离和测试保障”的状态。

如果后续继续演进，优先建议关注：
- 真实 GPU 联调与压测
- LLM 路径依赖固化
- 更细粒度的并发压测
- 如有需要，再考虑分布式任务队列
