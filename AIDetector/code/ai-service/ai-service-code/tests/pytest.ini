[pytest]
python_files = test_*.py
addopts =
    -ra
    --strict-markers
markers =
    unit: 纯函数 / 小范围逻辑单测
    integration: 走真实推理链路的集成测试
    slow: 单次 > 2s 的慢测试
    gpu: 需要 CUDA 的测试
    external: 依赖外部 LLM API 的测试
