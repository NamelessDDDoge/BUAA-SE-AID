# 远程更新清单

适用场景：
- 服务器上前后端已在运行
- 本地已完成代码修改
- 现在需要把新版本同步到远程环境

## 1. 同步前确认

先确认你本地已经完成：
- 后端关键测试通过
- 用户前端 `npm run build` 通过
- `.env` 新增配置项已整理好

建议至少确认这些配置项：

```env
IMAGE_TASK_MAX_WORKERS=2
RESOURCE_TASK_MAX_WORKERS=2
AI_SERVICE_PYTHON=/root/miniconda3/envs/se/bin/python
AI_REMOTE_INFER_URL=
AI_REMOTE_INFER_TIMEOUT=1800
AI_REMOTE_INFER_TOKEN=
AI_REMOTE_INFER_MAX_CONCURRENCY=1
```

## 2. 同步代码

在服务器项目目录执行：

```bash
cd /你的项目目录/BUAA-SE-AID
git pull
```

如果不是 `git pull`，用你当前的代码同步方式更新文件即可。

## 3. 后端需要做的事

进入后端目录：

```bash
cd AIDetector/code/backend/backend-code
```

### 必做

1. 检查 `.env`
2. 重启后端服务

说明：
- 这次改动主要在 Django 后端 Python 代码
- 不重启后端，新逻辑不会生效

### 一般不需要做

- 不需要 `python manage.py migrate`
  - 这轮没有新增 migration
- 不需要重装 Python 依赖
  - 这轮没有新增 requirements

## 4. 图像检测远程 GPU 模式需要做的事

如果你使用远程 GPU 服务：

1. 同步 AI service 代码
2. 在 GPU 所在机器重启 `gpu_infer_service.py`

建议确认启动参数：

```bash
export AI_SERVICE_PYTHON=/root/miniconda3/envs/se/bin/python
export AI_REMOTE_INFER_HOST=127.0.0.1
export AI_REMOTE_INFER_PORT=18080
export AI_REMOTE_INFER_TOKEN=你的随机串
export AI_REMOTE_INFER_MAX_CONCURRENCY=1
python gpu_infer_service.py
```

如果用了 SSH 反向隧道，也确认隧道仍然正常。

## 5. 前端需要做的事

这轮改动涉及用户前端页面逻辑，所以建议：

```bash
cd AIDetector/code/frontend/frontend-user
npm install
npm run build
```

然后按你的部署方式更新前端产物并重启前端服务。

说明：
- 如果服务器上的 `node_modules` 已完整且锁文件未变，`npm install` 有时可以省略
- 但第一次建议还是执行一次

管理员前端：
- 这轮核心改动不在管理员前端
- 一般不需要重新构建管理员端，除非你们的部署方式要求统一前端全量构建

## 6. 更新后最小验证

建议按下面顺序验证：

### 图像检测

1. 一次上传多张图
2. 提交检测
3. 去历史记录检查是否自动拆成多个图像任务
4. 随机点开一个任务看详情页和报告

### 论文检测

1. 一次上传多篇论文
2. 创建检测任务
3. 去历史记录检查是否自动拆成多个论文任务

### Review 检测

1. 上传一篇论文
2. 上传多篇 Review
3. 创建检测任务
4. 去历史记录检查是否自动拆成多个 Review 任务

## 7. 出问题时优先检查

### 后端不生效

优先检查：
- 后端是否真的重启
- `.env` 是否加载到正确值

### 图像检测失败

优先检查：
- `AI_SERVICE_PYTHON`
- `AI_SERVICE_DIR`
- `AI_REMOTE_INFER_URL`
- `AI_REMOTE_INFER_TOKEN`
- GPU 服务是否重启

### 资源检测任务创建行为不对

优先检查：
- 用户前端是否更新到最新构建产物
- 后端是否重启

## 8. 最小上线口径

如果你想最稳上线，建议先保持：

```env
IMAGE_TASK_MAX_WORKERS=2
RESOURCE_TASK_MAX_WORKERS=2
AI_REMOTE_INFER_MAX_CONCURRENCY=1
```

先确认功能正确，再逐步放量。
