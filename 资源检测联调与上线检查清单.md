# 资源检测联调与上线检查清单

本文档用于配合当前资源检测“受控并发 + 真批量资源处理”实现做联调、压测和上线前检查。

适用范围：
- `AIDetector/code/backend/backend-code`
- 论文检测 `task_type=paper`
- 同行评审 Review 检测 `task_type=review`

## 1. 当前实现的默认口径

当前代码默认口径是：
- 普通资源任务：走受控并发执行池
- 含 LLM 的资源任务：走独立串行执行池
- `paper` 任务：支持一个任务处理多个 `paper` 文件
- `review` 任务：支持一个任务处理多个合法 pair
- 多资源结果聚合在 `results.items`
- 原单资源字段继续保留，默认镜像第一个资源结果

这意味着：
- 后端已经具备资源级批量执行能力
- 前端已经具备多资源总览与切换查看能力
- 但第一次联调不要直接把资源任务并发度开太高

## 2. 推荐初始参数

建议后端 `.env` 至少补上：

```env
RESOURCE_TASK_MAX_WORKERS=2
```

如果 paper 任务会触发图像抽取并继续做图像检测，还应同时确认：

```env
IMAGE_TASK_MAX_WORKERS=2
AI_SERVICE_PYTHON=/root/miniconda3/envs/se/bin/python
AI_REMOTE_INFER_URL=
AI_REMOTE_INFER_TIMEOUT=1800
AI_REMOTE_INFER_TOKEN=
```

## 3. 无 GPU 环境下可以做什么

即使当前没有可用 GPU，仍然可以完成这些工作：

- 跑资源任务自动化测试
- 验证 paper/review 任务调度
- 验证资源任务并发分流
- 验证多资源聚合结果
- 验证状态接口与结果接口

当前可直接运行的关键回归命令：

```bash
cd AIDetector/code/backend/backend-code
env DATABASE_MODE=local AI_REMOTE_INFER_URL= AI_REMOTE_INFER_TOKEN= /root/miniconda3/envs/se/bin/python manage.py test core.tests.test_resource_task_concurrency
```

以及资源检测新能力组合回归：

```bash
env DATABASE_MODE=local AI_REMOTE_INFER_URL= AI_REMOTE_INFER_TOKEN= /root/miniconda3/envs/se/bin/python manage.py test \
  core.tests.test_resource_task_concurrency \
  core.tests.test_resource_preprocessing.ResourcePreprocessingTests.test_run_paper_detection_aggregates_multiple_paper_files_into_items \
  core.tests.test_resource_preprocessing.ResourcePreprocessingTests.test_run_review_detection_aggregates_multiple_review_pairs_into_items \
  core.tests.test_resource_task_flow.ResourceTaskFlowTests.test_paper_results_endpoint_exposes_multi_resource_items_when_present \
  core.tests.test_resource_task_flow.ResourceTaskFlowTests.test_review_task_status_exposes_multi_resource_items_when_present \
  core.tests.test_resource_task_flow.ResourceTaskFlowTests.test_paper_results_endpoint_preserves_items_when_dedicated_tables_exist \
  core.tests.test_resource_task_flow.ResourceTaskFlowTests.test_review_status_preserves_items_when_dedicated_tables_exist
```

这些测试不依赖真实 GPU。

## 4. 有 GPU 之前不要做的事

在没有可用 GPU、也没有完整模型权重时，不建议直接做：

- 开启 paper 抽图后图像检测，并期待真实模型结果
- 大规模提交含抽图图像检测的论文任务
- 调大资源任务并发度同时调大图像任务并发度

原因：
- 资源检测本身主要是文本链路
- 但 paper 任务一旦开启图片检测，最终还是会落到图像检测链路和真实模型推理

## 5. 论文检测联调检查

论文检测联调前，先确认：

1. 后端 `.env` 正常加载
2. `RESOURCE_TASK_MAX_WORKERS` 已设置
3. 如果要开图片检测：
   - 图像检测链路已可用
   - `AI_SERVICE_PYTHON` 已配置
4. 文档预处理依赖正常
5. 文本检测和参考文献分析配置正常

建议顺序：

1. 先提交单篇论文
2. 再提交一个包含 2 个 `paper` 文件的任务
3. 检查结果页是否能看到：
   - 第一份资源兼容视图
   - `items` 总览
   - 资源切换查看

## 6. Review 检测联调检查

Review 检测联调前，先确认：

1. 每个 `review_file` 都正确关联 `review_paper`
2. 后端 `.env` 已设置 `RESOURCE_TASK_MAX_WORKERS`
3. 如果使用 LLM Review 分析，相关模型配置可用

建议顺序：

1. 先提交一组 `review_paper + review_file`
2. 再提交一个含两组 pair 的任务
3. 检查结果页是否能看到：
   - 批量 Review 总览
   - 按资源切换查看

## 7. 上线前最低检查项

上线前至少确认：

- 资源任务并发测试通过
- 多资源 paper/review 聚合测试通过
- 资源状态接口可正常返回 `items`
- 单资源兼容字段仍正常返回
- paper/review 结果页可打开
- 如果有多资源任务，前端可切换查看
- 报告生成不报错

## 8. 推荐的逐步放量策略

### 阶段 1

- `RESOURCE_TASK_MAX_WORKERS=2`
- paper/review 含 LLM 任务继续默认串行

适合：
- 第一次联调
- 第一次上线
- 资源任务量中等

### 阶段 2

- `RESOURCE_TASK_MAX_WORKERS=3`

前提：
- 文本检测外部服务稳定
- paper/review 任务不会长期堆积
- 如果 paper 同时启图像检测，图像链路并发也要能承受

## 9. 当前剩余风险点

虽然本次改造已经补齐了资源检测的并发与批量主能力，但仍需注意：

- 资源专用 `OneToOne` 结果模型仍偏向第一个资源兼容语义
- 多资源前端展示当前是“兼容增强版”，不是全新多资源管理界面
- 资源预处理中仍有个别历史测试与当前实现存在漂移
- 含 LLM 的资源任务仍依赖外部模型/接口

## 10. 推荐收尾命令

建议至少执行：

```bash
cd AIDetector/code/backend/backend-code
env DATABASE_MODE=local AI_REMOTE_INFER_URL= AI_REMOTE_INFER_TOKEN= /root/miniconda3/envs/se/bin/python manage.py test core.tests.test_resource_task_concurrency
```

以及：

```bash
env DATABASE_MODE=local AI_REMOTE_INFER_URL= AI_REMOTE_INFER_TOKEN= /root/miniconda3/envs/se/bin/python manage.py test core.tests.test_resource_task_flow
```

如果做了前端改动，建议再执行：

```bash
cd AIDetector/code/frontend/frontend-user
npm run build
```

这可以作为当前资源检测改造完成后的最低收尾标准。
