# 图像检测联调与上线检查清单

本文档用于配合当前图像检测“受控并发批量执行”实现做联调、压测和上线前检查。

适用范围：
- `AIDetector/code/backend/backend-code`
- `AIDetector/code/ai-service/ai-service-code`
- 图像检测主链路

## 1. 当前实现的默认口径

当前代码默认口径是：
- 普通图像任务：走受控并发执行池
- 含 LLM 的图像任务：走独立串行执行池
- 一次提交多张图时，后端会自动拆成多个单独检测任务
- 本地推理：每次请求独立 request 目录
- 远程 GPU 推理：每次请求独立 request 目录
- 远程 GPU 服务并发度默认建议保持 `1`

这意味着：
- 代码已经具备并发安全基础
- 但第一次上线不要直接把并发参数开太大

## 2. 推荐初始参数

建议先使用以下参数启动：

后端 `.env`：

```env
AI_SERVICE_PYTHON=/root/miniconda3/envs/se/bin/python
IMAGE_TASK_MAX_WORKERS=2
AI_REMOTE_INFER_URL=
AI_REMOTE_INFER_TIMEOUT=1800
AI_REMOTE_INFER_TOKEN=
```

如果走远程 GPU 模式：

```env
AI_REMOTE_INFER_URL=http://127.0.0.1:18080/infer
AI_REMOTE_INFER_TIMEOUT=1800
AI_REMOTE_INFER_TOKEN=你自己设置的随机串
AI_REMOTE_INFER_MAX_CONCURRENCY=1
```

远程 GPU 服务启动参数：

```bash
export AI_SERVICE_PYTHON=/root/miniconda3/envs/se/bin/python
export AI_REMOTE_INFER_HOST=127.0.0.1
export AI_REMOTE_INFER_PORT=18080
export AI_REMOTE_INFER_TOKEN=你自己设置的随机串
export AI_REMOTE_INFER_MAX_CONCURRENCY=1
python gpu_infer_service.py
```

## 3. 无 GPU 环境下可以做什么

即使当前没有可用 GPU，仍然可以完成这些工作：

- 跑后端自动化测试
- 验证任务调度与状态流转
- 验证 request 目录隔离
- 验证远程桥接协议
- 验证结果解析和落库逻辑

当前可直接运行的回归命令：

```bash
cd AIDetector/code/backend/backend-code
env DATABASE_MODE=local AI_REMOTE_INFER_URL= AI_REMOTE_INFER_TOKEN= /root/miniconda3/envs/se/bin/python manage.py test core.tests.test_image_detection_flow core.tests.test_image_detection_concurrency
```

这些测试不依赖真实 GPU。

## 4. 有 GPU 之前不要做的事

在没有可用 GPU、也没有完整模型权重时，不建议直接做：

- 从前端提交真实图像检测任务并期待真实模型结果
- 手工执行真实 `local_infer.py`
- 启动 `gpu_infer_service.py` 后做真实模型联调
- 调大 `AI_REMOTE_INFER_MAX_CONCURRENCY`

原因：
- 这几步会触发真实模型加载
- 真实模型路径和显存约束不在自动化测试覆盖范围内

## 5. 本地模式联调检查

后端本地模式联调前，先确认：

1. `.env` 中没有误填 `AI_REMOTE_INFER_URL`
2. `AI_SERVICE_DIR` 指向 `AIDetector/code/ai-service/ai-service-code`
3. `AI_SERVICE_PYTHON` 指向可用环境
4. 模型权重路径已经准备好
5. 本机 Python 能正常 import 所需依赖

建议顺序：

1. 先跑自动化测试
2. 再启动后端
3. 再从前端提交 1 张图
4. 再尝试 2 张图
5. 检查历史记录中是否自动出现 2 个独立图像任务
6. 最后再试更多图片批量

## 6. 远程 GPU 模式联调检查

远程模式联调前，先确认：

1. 笔记本本地 `gpu_infer_service.py` 正常启动
2. 本地 `curl http://127.0.0.1:18080/health` 可通
3. SSH 反向隧道已建立
4. 服务器 `curl http://127.0.0.1:18080/health` 可通
5. 服务器 `.env` 中：
   - `AI_REMOTE_INFER_URL`
   - `AI_REMOTE_INFER_TOKEN`
   - `AI_REMOTE_INFER_TIMEOUT`
   配置正确
6. `AI_REMOTE_INFER_MAX_CONCURRENCY` 初始值为 `1`

建议顺序：

1. 单张图联调
2. 双张图批量联调
3. 检查是否自动拆成 2 个独立任务
4. 两个普通图像任务连续提交
5. 最后再尝试并发提交

## 7. 上线前最低检查项

上线前至少确认以下项目：

- 图像检测主链路自动化测试通过
- 并发隔离测试通过
- 后端 `.env` 中没有明显错误值
- 若走远程 GPU 模式，`health` 检查通过
- 首次真实任务可以完整跑通
- `DetectionTask` 状态能从 `in_progress` 正常到 `completed`
- 结果图、mask 图、报告文件都能正确生成

## 8. 推荐的逐步放量策略

### 阶段 1

- `IMAGE_TASK_MAX_WORKERS=2`
- `AI_REMOTE_INFER_MAX_CONCURRENCY=1`

适合：
- 第一次联调
- 第一次上线
- GPU 资源不稳定

### 阶段 2

- `IMAGE_TASK_MAX_WORKERS=2` 或 `3`
- `AI_REMOTE_INFER_MAX_CONCURRENCY=2`

前提：
- 单任务显存占用可控
- 双请求不会 OOM
- 模型加载和推理耗时稳定

### 阶段 3

如果要继续放量，建议先做压测，不要直接在线上硬调。

## 9. 压测建议

当前更推荐“功能性并发验证”，而不是极限压测。

建议压测方式：

1. 一次提交 2 到 5 张图片
2. 检查是否自动拆成对应数量的独立任务
3. 检查：
   - 是否都完成
   - 结果是否串任务
   - 生成文件是否覆盖
   - 后端是否卡死
4. 如果远程 GPU 模式稳定，再把 `AI_REMOTE_INFER_MAX_CONCURRENCY` 从 `1` 调到 `2`

不建议一上来就做：
- 大量任务同时提交
- 大批量图片直接压满
- 含 LLM 任务和普通任务混合高并发压测

## 10. 当前剩余风险点

虽然这次改造已经收敛了主要并发风险，但仍需注意：

- LLM 路径本身依赖重、启动慢
- LLM 路径仍有运行时依赖安装行为
- 真实 GPU 推理的显存上限不在自动化测试覆盖范围内
- 模型权重缺失或路径错误会在真实联调时暴露

所以当前最合理的上线策略是：
- 普通图像任务先上线受控并发能力
- LLM 任务继续保守使用

## 11. 推荐收尾命令

代码修改完成后，建议至少执行：

```bash
cd AIDetector/code/backend/backend-code
env DATABASE_MODE=local AI_REMOTE_INFER_URL= AI_REMOTE_INFER_TOKEN= /root/miniconda3/envs/se/bin/python manage.py test core.tests.test_image_detection_flow core.tests.test_image_detection_concurrency
```

如果这条命令通过，说明：
- 主链路回归正常
- 并发隔离保障正常

这可以作为当前图像检测改造完成后的最低收尾标准。
